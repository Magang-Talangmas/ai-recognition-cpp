services:
  ai-mobile-api:
    build: .
    container_name: ai-mobile-api
    command: python mobile_api.py
    restart: unless-stopped
    network_mode: host
    env_file:
      - .env

  ai-recognition-worker:
    build: .
    container_name: ai-recognition-worker
    command: python recognition.py
    restart: unless-stopped
    network_mode: host
    env_file:
      - .env
